import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Study",
    description: "Study",
};
export default function MemoryCard() {
    return (
        <>
            Memory card
        </>
    );
}
