import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Study",
    description: "Study",
};
export default function Notification() {
    return (
        <>
            Notification
        </>
    );
}
